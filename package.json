{
  "name": "farkle",
  "version": "1.0.0",
  "description": "Gioco di dadi Farkle — PWA + wrapper Android (Capacitor)",
  "private": true,
  "scripts": {
    "build:web": "node copy-web.mjs",
    "android:add": "npm run build:web && npx cap add android && npx cap sync android",
    "android:sync": "npm run build:web && npx cap sync android",
    "android:open": "npx cap open android"
  },
  "dependencies": {
    "@capacitor/android": "^6.1.2",
    "@capacitor/core": "^6.1.2"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.1.2"
  }
}
